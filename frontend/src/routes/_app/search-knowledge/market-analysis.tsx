import { createFileRoute } from "@tanstack/react-router"; import { KnowledgeWorkbench } from "@/modules/search-knowledge/components"; export const Route=createFileRoute("/_app/search-knowledge/market-analysis")({component:()=> <KnowledgeWorkbench title="Market Analysis" description="Understand country, language, trend, seasonality and regional search behaviour." controls={["Country and language opportunity profiles","Seasonality and search-trend tracking","Popular query clusters by market","Regional SERP behaviour comparison","Competition-level assessment","Market-specific content recommendations"]} ai="Demand in the primary market peaks six weeks earlier than the global trend; shift content publication forward."/>});
