import { createFileRoute } from "@tanstack/react-router"; import { KnowledgeWorkbench } from "@/modules/search-knowledge/components"; export const Route=createFileRoute("/_app/search-knowledge/approvals")({component:()=> <KnowledgeWorkbench title="Knowledge Approvals" description="Review governed search guidance before it becomes the team’s canonical reference." controls={["Pending review queue","SEO-owner assignment","Approve, reject and request revisions","Structured reviewer comments","Revision history","Publication decision evidence"]} ai="Four items are pending review; prioritize the algorithm-update interpretation because its effective date is today."/>});
