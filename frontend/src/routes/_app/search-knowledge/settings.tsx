import { createFileRoute } from "@tanstack/react-router"; import { KnowledgeWorkbench } from "@/modules/search-knowledge/components"; export const Route=createFileRoute("/_app/search-knowledge/settings")({component:()=> <KnowledgeWorkbench title="Search Knowledge Settings" description="Configure source policy, refresh cadence, review ownership and notifications." controls={["Source trust policy","Refresh cadence and alerts","Owner and reviewer assignment","Publication controls","Notification preferences","FastAPI-ready integration settings"]} ai="Weekly source refreshes are sufficient for stable documentation; enable rapid review alerts for algorithm updates."/>});
