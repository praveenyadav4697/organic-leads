import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/consent")({component:()=> <Workbench title="Consent Management" description="Manage consent wording, cookie categories, regional policy and evidence history." capabilities={["Necessary, analytics, marketing and preference categories","Banner wording, language, expiry and version history","GDPR, CCPA, LGPD policy mapping and cookie scanning"]} ai="The German consent wording requires legal review before the next publish window."/>});
