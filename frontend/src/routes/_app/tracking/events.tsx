import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/events")({component:()=> <Workbench title="Event Mapping" description="Define governed event contracts across every destination platform." capabilities={["Name, platform, trigger, parameters and category/action/label/value","Conversion goal, priority and lifecycle status","Create, edit, delete and clone versioned events"]} ai="AI detected three CTA clicks that can be normalized into a single generate_lead event."/>});
