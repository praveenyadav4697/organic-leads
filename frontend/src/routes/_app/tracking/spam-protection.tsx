import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/spam-protection")({component:()=> <Workbench title="Spam Protection" description="Layered form defense with transparent scoring and controllable policy." capabilities={["reCAPTCHA v2/v3, hCaptcha and Cloudflare Turnstile","Honeypot, Akismet, IP/country blocking and rate limits","Bot detection, spam score and policy exceptions"]} ai="The last seven blocked submissions share an automated browser fingerprint; raise the bot score threshold for this pattern."/>});
