import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/activity-logs")({component:()=> <Workbench title="Activity Logs" description="Searchable, exportable evidence across tracking, consent, forms and integrations." capabilities={["Audit, tracking, consent, form, webhook and integration logs","Advanced search, filtering and correlation ID lookup","CSV, Excel and PDF evidence exports"]} ai="No unexplained tracking configuration changes were detected in the last 24 hours."/>});
