import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/business-goals")({component:()=> <Workbench title="Business Goals" description="Manage measurable growth goals with clear accountability, value targets and progress." capabilities={["Traffic, SEO, sales, revenue, leads, brand, retention, email, social and paid categories","Priority, owner, start/end dates, current value, target and status","Goal details include expected ROI, budget, dependencies and notes"]} ai="The active organic traffic goal is 76% complete and remains achievable if the technical SEO backlog closes this sprint."/>});
