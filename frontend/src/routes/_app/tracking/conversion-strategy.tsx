import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/conversion-strategy")({component:()=> <Workbench title="Conversion Strategy" description="Design conversion goals, funnel stages and attribution policy across channels." capabilities={["Lead form, newsletter, call, WhatsApp, email, purchase, demo, appointment and download goals","Visitor → landing page → product → form → lead → customer funnel","First/last click, linear, position-based, time-decay and data-driven attribution"]} ai="The largest funnel drop occurs between product view and form start; test a shorter, contextual CTA."/>});
