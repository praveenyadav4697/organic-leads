import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/kpi-management")({component:()=> <Workbench title="KPI Management" description="Govern KPI definitions, targets, thresholds, ownership, cadence and performance trends." capabilities={["Users, sessions, page views, bounce/engagement rate and organic traffic","Keyword rankings, conversions, revenue, leads, CTR, CPA, CPC, ROAS and CAC","Trend, comparison and goal-progress visualizations"]} ai="Conversion rate is below its threshold; investigate the form step of the primary acquisition funnel."/>});
