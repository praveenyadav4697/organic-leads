import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/testing")({component:()=> <Workbench title="Testing Center" description="Validate event delivery and form behaviour before any production release." capabilities={["Fire test event and validate GTM and pixels","Test forms, preview mode and realtime debug","Pixel helper, console logs and verification results"]} ai="The LinkedIn tag test is failing because the conversion ID does not match the active campaign configuration."/>});
