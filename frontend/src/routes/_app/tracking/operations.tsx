import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/operations")({component:()=> <Workbench title="Operations" description="Operate reliable tracking and form jobs with traceability and recovery controls." capabilities={["Running, completed and failed job queue","Retry, cancel, escalation and execution timing","Correlation ID, trace ID and retry-count evidence"]} ai="Webhook deliveries to CRM are stable; the failed LinkedIn verification can be safely retried after credentials are refreshed."/>});
