import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/configuration")({component:()=> <Workbench title="Tracking Configuration" description="Govern platform connections, identifiers, owners, verification and health." capabilities={["GA4, GTM, Google Ads and Meta connections","Connection status, property and container identifiers","Connect, disconnect, enable, disable, refresh and verify"]} ai="Three platform mappings can be simplified by consolidating duplicate event parameters."/>});
