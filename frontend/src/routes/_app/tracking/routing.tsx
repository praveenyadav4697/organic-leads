import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/routing")({component:()=> <Workbench title="Routing Manager" description="Direct every qualified enquiry to the right team, system or workflow." capabilities={["Sales, support and marketing email destinations","HubSpot, Salesforce and Zoho CRM routing","Slack, Teams, Discord, Google Chat webhooks; priority, round-robin and fallback"]} ai="Route enterprise demo requests to the enterprise queue when company size exceeds 500 employees."/>});
