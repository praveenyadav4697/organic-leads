import { createFileRoute } from "@tanstack/react-router"; import { Workbench } from "@/modules/tracking-forms/components";
export const Route=createFileRoute("/_app/tracking/contact-forms")({component:()=> <Workbench title="Contact Form Manager" description="Design, validate, publish and observe high-quality conversion forms." capabilities={["Field library: text, email, phone, select, checkbox, radio, date and upload","Required fields, validation rules and conditional logic","Auto reply, email notification, webhook and CRM submission rules"]} ai="AI recommends making phone optional on the newsletter form to improve completion rate."/>});
